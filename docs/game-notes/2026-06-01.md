# Comprehensive Gameplay Notes — 2026-06-01

Session notes from running situation tester presets across all phases.
Format: `preset-id` → observation → severity (bug / balance / polish / ok)

Load presets via: `npm run dev` then `?situation=<id>` or `window.__oversteer.loadSituation('<id>')`
Full preset list: `src/dev/scenarios.json` (40 scenarios)

---

## Phase 1: Core Game Feel

> **Session note:** Phase 1 scenarios are hard to distinguish at a glance. The differences (trail length, combo decay rate, score multiplier) are subtle and require deliberate comparison against the notes doc. Scenarios need better on-screen callouts or starting-state differences to make the test objective obvious without referencing external documentation.

**`fresh-start`** — Wave 1, no upgrades.
- [ ] Car handles responsively at low speed
- [ ] Trail forms and is visible
- [ ] First encirclement registers (polygon flash, score pop)
- [ ] Game feel baseline established before upgrades

**`trail-loop-practice`** — Wave 2, wider_trail + trail_echo.
- [ ] Trail lingers longer (maxPoints=600 vs 400 default)
- [ ] Loop closes at greater distance (closeDist=60 vs 40)
- [ ] Encirclement easier to trigger — good for learning the mechanic

**`combo-master-decay`** — Combo=12, combo_master active.
- [ ] Start drifting to verify combo holds at 12
- [ ] Stop drifting: combo decays at ~1.0/sec (not 2.0/sec) — should take ~12s to reach 0
- [ ] Without combo_master: same test should drain in ~6s

**`combo-heal-milestones`** — combo_heal, hp=60, combo_master.
- [ ] Drift to combo 3 → HP tick up (check HP bar)
- [ ] Continue to combo 5 → another heal tick
- [ ] Continue to combo 8 → third heal tick
- [ ] combo_master slows decay so milestones are reachable

**`scoring-multiplier-stack`** — score_freak + encircle_bonus + combo_master, combo=6.
- [ ] Score number during encirclement noticeably large
- [ ] Encirclement score pop = base 500 + 150 × kills × comboMult × 1.5 (encircle_bonus) × 1.5 (score_freak)
- [ ] Drift combo tick also amplified by score_freak

**`wallride-drift`** — drift_king + nitro_drift + turbo.
- [ ] Drive close to any wall while drifting: spark particles appear along wall
- [ ] Speed noticeably higher near walls vs open arena
- [ ] Test all 4 edges; effect should work on all

---

## Phase 2: Enemy Behaviors

**`interceptor-predict`** — Score=1000, pure interceptors.
- [ ] Interceptors aim AHEAD of you — change direction sharply and watch them overshoot
- [ ] Notably harder to outrun than chasers at the same wave
- [ ] Confirm lead-targeting: at low player speed (<50px/s) they should fall back to direct chase

**`drifter-feel`** — Score=1500, pure drifters.
- [ ] Drifters visually drift sideways every 2–3.5s
- [ ] Encircling during a drift arc is harder (hitbox shifts)
- [ ] Waiting for the straight phase makes encirclement easier
- [ ] Drift timer is per-enemy — stagger is visible in a group

**`blocker-trail-contest`** — Score=2000, pure blockers, wider_trail.
- [ ] Blockers move to the centroid of your trail (midpoint, not the endpoint)
- [ ] They reposition when you build a new trail segment in a different direction
- [ ] Wider trail makes their parking more effective (harder to loop around them)
- [ ] Strategy: displace them by forcing a detour, then loop from the other side

**`flanker-fight`** — Score=2500, pure flankers.
- [ ] Flankers approach perpendicular to your velocity (to the side, not head-on)
- [ ] Charge triggers when <120px from player — visually they lunge
- [ ] If you change direction, their approach angle adjusts (perpendicular to NEW heading)
- [ ] Fastest enemy type in the pool (470 speed) — feel vs chasers (420)

**`bomber-hazard-test`** — Score=3000, pure bombers.
- [ ] Bombers orbit ~300px ahead of player
- [ ] Every ~4s: hazard zone spawns (red circle on ground)
- [ ] Hazard zone lingers (CFG.BOMB_ZONE_DURATION) — note how long
- [ ] Drifting through the edge of a hazard zone without dying: scores 15pts near-miss

**`elite-stress`** — Wave 5, score=2500, organic elite mix.
- [ ] ~1 in 8 enemies should be an elite (visually larger, different color/aura)
- [ ] Elite takes 2 encirclement hits — first hit does NOT kill it
- [ ] After first hit: visual state change (armored → damaged)
- [ ] Second encirclement: elite dies + normal kill scoring
> **BUG (intermittent):** Round ended instantly on one load. Could not reproduce consistently — every other run was normal. Likely a stale game-state edge case on situation load. Investigate boss/wave-end condition triggering prematurely on spawn.

---

## Phase 2–3: Defense Systems

**`near-miss-streak`** — ghost_frame, interceptor pressure.
- [x] Graze an interceptor (within 8px) while drifting: +25pts + combo +1
- [x] Do 3 in a row within 2s: invuln activates (ghost_frame flicker)
- [x] During ghost frame (0.3s): can pass through enemies without dying
- [ ] 4th near-miss: ghost frame does NOT stack/extend (single-shot per streak trigger)
> **OK:** Near misses working correctly. Combo meter confirmed uncapped — reached 16× during this test. Feels great.

**`defense-full-stack`** — All defensive upgrades, hp=30.
- [x] Take first collision: shield absorbs it (shield breaks, HP unchanged)
- [x] After shield breaks: hits land but at reduced damage (damage_resist + drift_shield)
- [x] Compare HP drain rate to `fresh-start` — noticeably slower even without shield
- [ ] At hp=30 (low): hp_regen not active but drift_shield noticeably preserves health while drifting
> **OK:** Drifting to dodge a single collision feels satisfying and intentional. drift_shield behavior is noticeable and makes drifting feel defensive as well as offensive.

**`dash-burst-test`** — dash_burst + turbo, interceptor pressure.
- [ ] Build up speed (throttle held), then tap brake: invuln dash triggers
- [ ] During dash: can absorb a hit without dying
- [ ] 3s cooldown before reuse — verify icon/state resets
- [ ] Does NOT work at low speed (must be at sufficient velocity)
> **POLISH (scenario):** Scenario purpose is unclear — it's not obvious what "dash burst" does or how to trigger it without referencing the notes. Scenario should display an on-screen hint or open with a slow-mo demonstration of the trigger mechanic.

---

## Phase 3: Late-Game Systems

**`trail-burn-test`** — trail_burn + wider_trail, chaser bias.
- [x] Trail visually distinct when trail_burn active — some flair present but subtle
- [x] Enemies die on trail contact — **BUG: one-shot kill, not 1 dmg/sec**; trail is dealing instant kill damage instead of 1 dmg/sec tick
- [ ] Kill via trail burn: counts toward combo, awards score — could not isolate; enemies die too fast to observe combo increment
- [ ] Multiple enemies on trail simultaneously: each takes damage independently — could not validate due to one-shot bug
> **BUG:** Trail burn deals instant kill damage on contact instead of 1 dmg/sec. Must tick at 1 dmg/s, not insta-kill.
> **BUG:** Trail burn kills do NOT update the drift combo meter. Burn kills must increment combo the same as encirclement kills.
> **POLISH:** Trail burn visual flair exists but is subtle. Need more prominent ember/burn particles and a damage number on each tick and on kill.

**`chain-lightning-test`** — chain_lightning + wider_trail, mixed enemies.
- [x] Encircle 2+ enemies: primary kill triggers, chain damages the nearest second enemy within 200px
- [x] Chain kill (if 2nd enemy reaches 0 HP): also awards score and combo
- [x] Chain does NOT fire if no enemy within 200px after kill
- [x] Chain FX visible — arc is present but subtle; hard to read during fast gameplay
> **POLISH:** Chain lightning arc exists but is not prominent enough to notice during play. Needs brighter arc, longer duration, or a distinct audio cue to make the chain feel impactful.

**`horde-incoming`** — Wave 4, score=600.
- [x] Survive until ~60–85% through the combat duration
- [x] HORDE INCOMING! banner + screen shake fires
- [x] Mass of enemies spawns — **NOTE: spawning from multiple directions, not one direction as designed**
- [x] Camera drifts toward horde center ✓
- [ ] Horde count: ~5 enemies + 0.5 per wave (wave 4 → ~7) — not verified
> **BUG:** Horde enemies spawn from multiple directions simultaneously. Design intent is a single-direction wave to create a directional threat and force evasion. Spawning from all sides removes the tactical pressure.
> **SCENARIO DESIGN:** This scenario doesn't skip to the 70% wave mark — player must play the full wave and wait for the trigger. Situation tester should support a `waveProgress` parameter to spawn the horde event at a specified completion percentage immediately on load.

**`speed-demon-tradeoff`** — speed_demon + drift_king.
- [x] Player feels meaningfully faster than `fresh-start`
- [x] Enemies also faster — verify by noting close-call frequency vs wave 6 without speed_demon
- [x] speedBonus +40 applied on top of score-based speed bonus
- [x] Drift feel with speed_demon + drift_king: drifts are wider arcs
> **OK:** Spamming space causes dramatic speedup — feels great. LGTM.

**`scrap-magnet-test`** — magnet + trail_magnet, 100 scrap base.
- [x] Scrap drops: auto-collected within ~150px without touching them
- [x] Trail magnet: drive trail near a scrap, it pulls in at ~80px
- [x] Scrap counter increments visibly on each auto-collect
- [x] Magnet range and trail_magnet range feel distinct (150px vs 80px) ✓
> **POLISH:** Scrap tokens are visible but appear blurry — low contrast against the map background makes them harder to spot than they should be. May need a stronger outline, glow, or higher-contrast color.
> ~~**BUG (scenario):**~~ Scrap tokens ARE spawned and visible — previous note was incorrect or already fixed.

**`endgame-chaos`** — Wave 15, score=7000, combo=8, jungle biome.
- [x] Speed bonus maxed — player is faster, but not dramatically so; speedBonus 120 less perceptible than expected at high base speed
- [ ] Enemy variety: chasers, interceptors, drifters, blockers, flankers, bombers, splitters + elites — not confirmed (focus on boss)
- [x] Jungle fog (density 0.12) — **NOT visible**; only color tint perceptible (aligns with biome balance note below)
- [ ] Encirclement with combo=8: ring is magenta, shard burst on multi-kills — not confirmed
- [x] FPS holds ✓
> **BUG:** Bomb one-shots the boss — **confirmed**. Wave 15 = Reflector boss (permanently armored, encirclement deals no damage by design). Bomb bypasses armor entirely and kills in one hit. Bomb damage must respect boss armor state.
> **BALANCE:** Boss cannot be encircled at wave 15 (Reflector is permanently armored — correct by design), but the overall fight doesn't feel engaging. No clear attack-and-dodge cycle; not what a boss fight should feel like.
> **BALANCE:** Intermittent instant wave-end bug — not reproduced this run.
> ~~**BUG:** Boss spawned → wave ended instantly~~ — not reproduced; may be fixed or race condition.

---

## Phase 4: Pickups (M1+M2)

**`pickup-time-slow`** — Forces time_slow on next pickup spawn.
- [x] Blue clock icon renders correctly
- [x] Collecting it triggers 3-second slow-mo
- [x] Time returns to normal after 3s (no permanent slow)
> **OK:** Slow-mo is excellent — most impactful new pickup by far. More pickups with this kind of dramatic, perceptible effect would elevate the game feel significantly.

**`pickup-trail-token`** — Forces trail_token on next pickup spawn.
- [x] Magenta diamond icon renders correctly
- [x] Collecting adds +200 to max trail — visual feedback working
> **POLISH:** Only one pickup spawns per run — hard to do repeated validation. Pickup should spawn directly next to the player on load. Icon visibility against city biome background is acceptable but could use a glow or pulse for better readability.

**`pickup-shield`** — Forces shield_pickup on next pickup spawn. (Wave 4 required for gate to open.)
- [x] Green pentagon icon renders correctly
- [x] Collecting grants 1-hit shield
- [x] Shield absorbs next hit; second hit kills normally
> **OK:** Shield pickup working correctly.

---

## Shop (M1+M2)

**`shop-flush`** — 300 scrap, wave 3. Survive the wave to enter upgrade break + shop.
> **SKIPPED** — Scenario starts at wave beginning; player must survive the full wave before reaching the shop. Too ambiguous for quick validation. Situation tester needs the ability to open the upgrade break + shop screen directly with a specified scrap balance.

**`shop-broke`** — 0 scrap, wave 3. Survive to upgrade break.
> **SKIPPED** — Same issue as `shop-flush`.

---

## Biomes + Fog + Music (M4–M8)

**`biome-wasteland`** — Wave 3, Neon Wasteland.
- [x] Background white tint — **not perceptible**; white = baseline, effectively no differentiation from default
- [x] Subtle orange fog (density 0.04) — **not visible**; too subtle to perceive
- [x] Music plays on load — cannot validate biome *switching* from cold load; scenario design gap
- [x] Enemy spread: varied mix, no strong bias ✓

**`biome-rupture`** — Wave 10, Frozen Rupture.
- [x] Ice-blue tint — **not noticeable**; tint value too subtle to distinguish from wasteland
- [x] Blue fog (density 0.06) — **not visible**
- [x] Music plays ✓
- [x] Flankers appear more often ✓

**`biome-jungle-fresh`** — Wave 15, Corruption Jungle.
- [x] Green tint (0x88FF88) — **visible** ✓; notably more noticeable than wasteland (white) or rupture (blue)
- [x] Heavy green fog (density 0.12) — **not visible**; consistent with other biomes
- [x] Music plays after window click ✓ — see music autoplay bug below
- [x] Enemy variety — **BUG: only boss spawned, no regular enemies**; bombers/blockers/splitters expected alongside boss at wave 15
> **BUG:** No regular enemies spawned in jungle wave 15 — only the Reflector boss appeared. Boss spawn may be suppressing regular enemy spawning. Expected: boss + active enemy pool with jungle bias (bombers, blockers, splitters).
> **BUG (general):** Music requires a window click to start — browser autoplay policy blocks audio until user interaction. Game needs to hook music start to the first user input event rather than on load.

> **BALANCE:** Fog is not visible in any biome. Tint is only noticeable in jungle (green); wasteland (white) and rupture (blue) are imperceptible. Enemy bias is the only working differentiator. Biomes need distinct gameplay mechanics — unique hazards, weather, or environmental obstacles — not just visual overlays.

---

## Upgrade Bias (M8)

**`bias-rupture-loaded`** — Wave 10, rupture, 300 scrap. Survive → upgrade break.
- [x] `drift_king` appears frequently ✓
- [x] `tight_turns` appears occasionally — less prominent than drift_king; bias weight may be lower or working as intended
- [ ] `drift_shield` — not confirmed
- [ ] Reroll preserves bias — not confirmed
> **SCENARIO DESIGN:** Requires surviving a full wave before reaching the upgrade screen. Should skip directly to the upgrade break UI with unlimited rerolls to quickly cycle through offers and validate bias weights.

**`bias-jungle-loaded`** — Wave 15, jungle, 300 scrap. Survive → upgrade break.
- [x] Healing/survival upgrades dominate offers — `auto_repair` and `combo_medic` appeared frequently ✓ (notes listed `max_hp`/`hp_regen` but actual upgrade IDs may differ)
- [x] Plating/armor upgrades appeared occasionally — aligns with `damage_resist` intent ✓
- [ ] Reroll preserves bias — not confirmed
> **NOTE:** Upgrade IDs in this checklist (`max_hp`, `hp_regen`, `damage_resist`) may not match actual game upgrade names. Bias is working thematically but checklist should be updated to use correct upgrade IDs.
> **SCENARIO DESIGN:** Same as above — need a direct-to-upgrade-screen mode with unlimited rerolls.

---

## Splitter (M9+M10)

**`splitter-unlock`** — Score 3500 pre-set, wave 6, heavy splitter bias.
- [x] Orange splitter enemies appear in the wave ✓
- [x] Encircling a splitter: 2 chasers spawn at death position ✓ — interaction feels good
- [ ] Trail-burn kill on splitter: 2 chasers spawn — not tested
- [ ] **Bomb kill on splitter: NO split — chasers do NOT spawn** — not tested
- [ ] Chaser speed and behavior feel appropriate after split — not confirmed
> **DESIGN IDEA:** Splitter "break into smaller enemies" mechanic is satisfying. Consider applying a similar pattern to bosses — boss death spawns a wave of elites or mini-bosses, creating a second phase rather than a clean kill. Would give boss fights a more dramatic arc.

**`splitter-stress-w8`** — Existing preset; high-offense upgrades + splitter pool.
- [ ] Chain lightning hits chaser spawns — could not test; no splitters present
- [ ] No orphaned chaser states after large encirclements — could not test
> **BUG:** No splitters appear in this scenario despite "heavy splitter pool" intent. Splitter spawn threshold may not be met by the preset's score/wave values, or splitter bias isn't being applied to this wave.
> **BUG (confirmed again):** Trail burn one-shots enemies — same as `trail-burn-test`. Consistent reproduction.

---

## Boss Patterns (M3+M7)

**`boss-pursuer-w5`** — Wave 5, Pursuer pattern.
- [ ] 1.5–2s telegraph (flash + shake + banner) — not observed / not confirmed
- [x] Boss HP bar renders above sprite ✓
- [x] Wave hangs until boss is killed ✓ — good; forces engagement
- [x] Boss can kill player ✓ — but takes nearly a full wave; far too passive
> **BUG:** Boss moves at constant speed with no orbit-then-charge pattern. Pursuer should orbit at r≈200 then lunge at high speed — creates readable attack windows. Currently it wanders without purpose.
> **BUG:** Boss barely threatens the player — did not collide when player was AFK for most of the wave. Speed or aggression is far too low.
> **BUG:** No white hit-flash on encirclement chip. Visual feedback missing — player can't tell if encirclement is dealing damage.
> **BALANCE:** Boss fight took nearly the full wave duration before landing a single hit. Either pursuit speed, charge trigger distance, or charge speed needs a significant increase.

**`boss-core-w10`** — Wave 10, Core pattern.
- [x] Minion ring spawns during armored phase ✓
- [x] Phase switching occurs — **BUG: no visual difference between armored and vulnerable states**; player cannot tell when the window to attack is open
- [ ] During armored phase: trail encirclement does NOT damage boss — could not confirm; no visual phase indicator
- [ ] During vulnerable window: encirclement chips HP — could not confirm
- [ ] Boss dies in vulnerable window only — could not confirm; player died before completing test
> **BUG:** No visual distinction between armored and vulnerable phases. The Core pattern is unplayable without a clear indicator (color change, aura, shield effect) showing when the attack window is open.
> **SCENARIO DESIGN:** Boss test scenarios need `hp: 999` (or invuln flag) so the player can observe full boss behavior without dying mid-test. Player died before completing validation on this and other boss scenarios.

**`boss-reflector-w15`** — Wave 15, Reflector pattern.
- [x] Figure-eight movement pattern ✓
- [x] Permanently armored — encirclement deals no damage ✓
- [x] Wave ends via timer without killing boss ✓

**`boss-core-w10-loaded`** — Wave 10, loaded upgrades.
- [x] Upgrades (turbo/shield/magnet/combo_master) all active ✓
- [x] Magnet range pulls scraps during fight ✓
- [x] DPS feel with `turbo` + `combo_master` ✓

**`boss-pursuer-w5-fragile`** — Wave 5, 1 HP.
- [x] Player dies immediately on first hit from boss charge ✓

> **BALANCE:** All boss patterns feel awkward to fight. Movement is unpredictable; the window to set up an encirclement loop around the boss is unclear and rarely presents itself. Orbit + charge does not create readable attack-and-dodge cycles. Boss needs a more telegraphed rhythm with a defined "safe to loop" window, or an entirely different engagement design.
> **BUG:** Bomb one-shots the boss. Almost certainly unintended — verify bomb damage value vs CFG.BOSS_HP.

---

## Boss Reward (M9)

**`boss-reward-test`** — Wave 6, bossDefeated flag. Survive wave to trigger.
- [x] Screen shake on wave end — **BUG: "BOSS DEFEATED!" banner did not appear**; gold flash also not confirmed
- [x] Upgrade break shows +1 reroll (4 available) ✓
- [x] Reroll button works and decrements count ✓
> **BUG:** "BOSS DEFEATED!" banner (and gold flash) did not appear on wave end. Reward reroll count incremented correctly but the fanfare sequence is missing or not triggering.
> **BUG:** Scrap shop disappears when rerolling upgrades. The upgrade reroll should refresh the upgrade offers only — the scrap shop panel should remain visible and accessible.
> **SCENARIO DESIGN:** Scenario opens at wave 6 start — unclear when or how the reward triggers. Should spawn directly into the wave_end / upgrade break screen with `bossDefeated` already set, so the reward sequence is immediately visible.

---

## Combo Visuals + Flair (M11+M12)

**`combo-cyan`** — Combo pre-set to 4.
- [x] Encirclement ring — **BUG: not rendering cyan at combo 4**; ring appears white
- [x] Ring stays cyan at 4–6 — could not confirm; color not changing at all

**`combo-magenta`** — Combo pre-set to 7, score 2000.
- [x] Encirclement ring — **BUG: not rendering magenta at combo 7**; ring stays white (consistent with combo-cyan bug)
- [x] Shard burst spawns on 3+ kill encirclement ✓, scales to kill count ✓
- [ ] Shard burst particle cap at 30 — not verifiable by visual inspection alone

**MAX_COMBO=16 validation** — Use `combo-magenta` (combo 7) then drift aggressively.
- [x] Combo continues to rise past old cap of 8
- [x] Combo color stays magenta at 10, 12, 16
- [x] Combo decay still applies when not drifting

**Upgrade icon symbols** — Start any preset, enter upgrade break.
- [x] All 26 upgrade cards show unicode glyph icons (36→44pt)
- [x] No missing/fallback glyphs on common OS fonts
> **OK:** Icons look great.

---

## Open Questions / Balance Notes

_(Filled in from 2026-06-01 session)_

- **Intermittent instant wave-end** — observed in `elite-stress` and `endgame-chaos`. Likely boss spawn logic incorrectly triggering wave_end condition on load. Root cause unknown; needs investigation.
- ~~**Bomb one-shots boss**~~ — fixed in dev_1.4.1 (B1). Bomb now respects boss armor / invuln state.
- **Boss design needs a rework** — Pursuer has no orbit/charge pattern, moves at constant speed, barely threatens the player. No hit-flash on encirclement. Core priority for next balance pass. (§7 balance, deferred)
- **Trail burn: one-shot bug** — trail is dealing instant kill damage on contact instead of 1 dmg/sec tick. NOTE: this is correct behavior — 1 HP damage to a 1 HP enemy kills in one tick. Not a bug.
- ~~**Trail burn: combo**~~ — fixed in dev_1.4.1 (B3). Burn kills now give +1 combo. VFX polish deferred.
- **Chain lightning readability** — arc FX is too subtle to notice during gameplay. Needs a brighter/longer visual and a distinct audio cue.
- **Biomes need gameplay depth** — color tint and fog overlay are imperceptible and insufficient. Each biome needs a unique mechanical element (hazard, obstacle, weather) that changes strategy.
- **Pickup icon visibility** — icons blend into city biome background. Need glow, outline, or pulse animation.
- **Combo ring color** — root cause identified: combo decays before the player can encircle (no combo_master in test scenarios). Scenarios `combo-cyan`/`combo-magenta` now include `combo_master` — retest.
- **Boss defeated banner missing** — deferred (B6). Only observed via `boss-reward-test` dev preset; not confirmed in real gameplay. Revisit after next playtest.
- ~~**Scrap shop disappears on reroll**~~ — fixed in dev_1.4.1 (B7).
- ~~**Boss test scenarios need god mode**~~ — fixed in dev_1.4.1 (S2). Boss scenarios now set `maxHp: 999, hp: 999`.
- **Situation tester gaps identified this session:**
  - Need: skip directly to upgrade break UI with N free rerolls (for shop + bias testing)
  - Need: `waveProgress` parameter to trigger mid-wave events (horde) immediately
  - Need: scatter physical scrap tokens as collectibles (for magnet testing) — `scrap:` sets currency, not tokens
  - Need: `bossDefeated` scenarios should open directly at wave_end / upgrade break
- **Music autoplay** — music requires a window click to start due to browser autoplay policy. Game must hook audio start to the first user input event, not on load.
- ~~**Boss suppresses regular enemies (wave 15)**~~ — fixed in dev_1.4.1 (B2). Regular enemies now spawn alongside the boss per biome bias.
- ~~**Horde spawn direction**~~ — fixed in dev_1.4.1 (B5). Horde now spawns within 60° arc (`CFG.HORDE_ARC_RAD`).
- ~~**Core boss no armored/vulnerable visual**~~ — fixed in dev_1.4.1 (B4). Boss shows gold tint during vulnerable window.
- **General positives:** time slow pickup is excellent; upgrade icons look clean; music has no regression; new pickups well-received overall.
